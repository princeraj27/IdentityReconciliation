{
  "name": "bitespeed-identity-reconciliation",
  "version": "1.0.0",
  "description": "Bitespeed Identity Reconciliation Service - Links customer identities across multiple purchases",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx prisma migrate deploy && npx prisma generate && tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "postinstall": "npx prisma generate && npx prisma migrate deploy || true"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.9.0",
    "prisma": "^5.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}